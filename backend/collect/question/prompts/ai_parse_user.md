输出格式硬性要求：VALID_JSON_ONLY；只返回一个完整 JSON 对象 {"questions":[...]}；第一个非空字符必须是 {，最后两个非空字符必须是 ]}；禁止 markdown、解释文字、尾随逗号和半截 JSON。返回前必须确认 JSON.parse 可直接解析。

完整性硬性要求：先在内部识别本段所有题号和空号，再输出 JSON。标题、书名、卷号、章节名、栏目名不是题目；比如“小升初英语复习题三十套（含详细解析）”“（一）”“一、语法巩固”不能单独生成 question。答案区编号只用于回填 answer_key 和 analysis_text，不能作为新增题目。如果题干区有 1-6，答案区又有 1-6 解析，最终必须输出 6 条 question，不是 12 条。

题型硬性要求：
- 普通编号题 1、2、3... 必须逐题输出。
- 单项选择连续编号不能漏最后一题；如果题干区出现 1-6，答案区也有 1-6，则必须输出 6 条 question。
- “根据提示完成句子”“句型转换”“对划线部分提问”都按 blank + fill_word 输出，不要改成 short_answer 或 normal。
- 补全对话/选句填空/七选五如果出现 ____1____ 到 ____5____ 或答案区 1.B 2.D 3.E 4.C 5.A，必须输出 5 条独立 question，title 和 stem_text 用“补全对话第N空”区分，禁止合并成 1 条 reading_choice，禁止只输出第 1 空。
- “用适当的句子完成对话”没有候选项时，也必须按每个空单独输出；如果答案区用“一、用适当的句子完成对话。1. ... 2. ...”列答案，必须回填，不能留空。若答案区只有标题、没有实际答案行，不要自行补写或推断开放性答案，answer_key 留空。
- 对话 1-5 后面还有阅读理解 1-5 时，即使答案全部缺失，也要输出 5 条对话题和 1 条阅读父题；只让 answer_key 为空，不要返回空 questions。
- 答案只从明确答案区回填；先建立答案区编号映射，再回填答案。答案区缺失、只有标题、没有编号答案行、或被截断时，不要自行解题，只把 answer_key 留空，仍必须输出所有题目，严禁返回空 questions。若答案区只给出 1-4，则只回填 1-4，5 及之后留空。选择题、阅读理解、完形填空、句型转换、补全对话都不能根据短文或常识自行选择答案；只有答案已经明确印在原文答案区时才填写 answer_key。
- analysis_text 只从答案区对应题号的“解析”原文或原文关键句回填；不要自行改写、扩写或补充解析。没有明确解析时留空。
- 反例：没有答案行“5. So do we”时，不得把 So do we 写入第 5 题；没有阅读答案行时，不得写 57:D、58:D 等阅读答案；只有“用适当的句子完成对话”答案小标题、没有 1-5 答案行时，不得补写 What can I do for you?、It's forty dollars. 等推断答案。
- 反例：阅读回答问题只有“答案及解析”标题、没有 1-5 答案行时，不得补写 English、Over 6,000、Women 等从文章推断出的答案。
- 阅读理解选择题必须输出 1 条父 question，并把所有小题放进 choice_items，不能把每个小题拆成多条父 question。
- 选出不同词/不同类必须按 1-5 每题一条 single_choice，不要合并；标题“选出不同的词”不是题，禁止只输出 1 条 stem_text 为“选出不同的词”的总题。每条 stem_text 必须包含该编号题 A/B/C/D 四个选项。如果后面还有完形填空，最终通常是 5 条词汇题 + 1 条完形父题。
- 遇到 PDF 粘连如 D. famous at8. 或 D. keep9.，要拆成上一题 D 选项和下一题题号。

{{if gt .ChunkTotal 1 -}}
以下是长 PDF 按试卷/题组边界拆分后的第 {{.ChunkIndex}}/{{.ChunkTotal}} 段。请从头到尾完整解析本段所有完整题目，保持题号、答案和解析对应。只有开头或结尾确实被截断、无法还原的残缺片段才跳过；不要因为题目数量多而省略中间题目。返回固定 JSON：
{{else -}}
请从头到尾完整解析以下题目文本，返回固定 JSON；不要抽样或省略题目：
{{end}}

{{.RawText}}
